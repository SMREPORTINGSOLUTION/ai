import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Toaster } from "@/components/ui/toaster"
import { AuthProvider } from "@/contexts/auth-context"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "iPhone 15 Daily Contest - Win 100 iPhones Every Day!",
  description:
    "Join 100,000 participants daily for a chance to win one of 100 brand new iPhone 15 devices. Free entry, instant confirmation, worldwide shipping.",
  keywords: "iPhone 15, contest, giveaway, free, daily, win, smartphone",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
          <Toaster />
        </AuthProvider>
      </body>
    </html>
  )
}
